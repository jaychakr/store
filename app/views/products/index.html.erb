<h1>Products</h1>

<%= link_to "Add a New Product", new_product_path %>
<br><br>
Current products:
<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>